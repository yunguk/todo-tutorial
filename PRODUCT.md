# PRODUCT.md

## 대상 사용자
개인 생산성 관리자 — 혼자서 일상적인 할 일을 관리하고 싶은 사람.

## 문제
메모장, 노션 같은 범용 도구는 자유도가 높은 대신 할 일 관리에 필요한 구조가 없다.
그 결과 할 일이 다른 메모 사이에 묻히거나, 완료 여부를 추적하지 못해 관리가 되지 않는다.

## 지금 쓰는 대안
메모장, 노션 등 범용 문서/메모 도구.
전용 할 일 관리 기능이 없어 목록이 뒤섞이고 체크 상태 관리가 번거롭다.

## 약속할 변화
할 일을 놓치지 않게 된다.
구조화된 목록과 완료 처리로, 적어둔 일이 흐지부지 사라지지 않고 끝까지 이어진다.

## 반복되는 사용자 흐름
할 일 추가 → 체크 → 다음 날 반복.
이 흐름이 가장 자주, 가장 빠르게 반복될 수 있어야 한다.

---

# 신규 기능: 고양이 상식 목록

## 대상 사용자
할 일 관리 틈틈이 가벼운 콘텐츠를 보고 싶은 사용자.

## 문제
할 일 목록만 있으면 앱이 딱딱하고, 사용자가 앱을 자주 열 유인이 적다.

## 지금 쓰는 대안
별도 웹사이트나 SNS에서 고양이 관련 콘텐츠를 찾아본다.
앱을 벗어나야 하므로 흐름이 끊긴다.

## 약속할 변화
앱 안에서 짧은 휴식처럼 고양이 상식을 확인할 수 있게 된다.

## 반복되는 사용자 흐름
목록 진입 → 항목 클릭 → 상세 설명 확인 → 목록으로 복귀.

## 데이터 소스
Cat Facts API: https://alexwohlbruck.github.io/cat-facts/
- `GET https://catfact.ninja/facts` 등으로 목록 조회
- 각 항목은 fact 텍스트(및 length)를 포함

## 화면 요구사항
- 목록 화면: 고양이 상식 여러 건을 리스트로 표시 (요약/첫 줄만 노출 가능)
- 항목 클릭 시: 선택한 항목의 전체 설명(fact 전문)을 보여주는 상세 화면 또는 확장 영역 표시
- Server Components 우선, 상세 내용 표시를 위한 클라이언트 상태는 최소화 (예: 선택된 항목 id만 클라이언트 상태로 관리)

## 배포 상태
완료 및 배포됨 (`/cat-facts`). 구현 상세와 완료 기준은 이 기능 개발 중 `docs/specs/cat-facts/spec.md`에 정리했으며, 배포 완료로 해당 스펙 폴더는 정리(삭제)했다.

---

# 신규 기능: 고양이 O/X 퀴즈

## 대상 사용자
고양이 상식을 재미있게, 능동적으로 확인하고 싶은 사용자.

## 반복되는 사용자 흐름
문제(한글 문장) 확인 → O/X 선택 → 정답 여부 확인 → (틀리면 다시 풀기 또는) 다음 문제 → 5문제 후 점수 확인 → 다시 시작.

## 데이터 소스와 결정 사항
- 참(O) 문장: Cat Facts API(`https://catfact.ninja/facts`) 원문.
- 거짓(X) 문장: API가 거짓 진술을 제공하지 않으므로, 알려진 사실 10개에 대해 정적으로 작성한 거짓 변형을 `lib/cat-quiz.ts`에 매핑해 사용.
- 번역: MyMemory 무료 번역 API(`api.mymemory.translated.net`, 키 불필요). 실패 시 영문 원문으로 폴백.
- 문제 위 랜덤 고양이 사진: TheCatAPI(`https://api.thecatapi.com/v1/images/search`) 사용. 원래 요청받은 `aws.random.cat/meow`는 응답이 불안정(503/연결 실패 반복)해 교체함. 실패 시 사진 없이 문제만 표시.

## 화면 요구사항
- 한 번에 한 문제씩 진행, O/X 선택 즉시 정답 여부 표시.
- 오답 시에만 "다시 풀기" 버튼으로 같은 문제 재도전 가능 (정답 시 점수 반영, 재도전 전 오답은 반영 안 함).
- 마지막 문제 이후 최종 점수와 "게임을 다시 할까요?" 버튼(초기 상태로 재시작) 표시.

## 배포 상태
완료 및 배포됨 (`/cat-quiz`). 구현 상세와 완료 기준은 개발 중 `docs/specs/cat-quiz/spec.md`에 정리했으며, 배포 완료로 해당 스펙 폴더는 정리(삭제)했다.
